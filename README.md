# Appscrip-task-YourName

This repository contains the implementation of the **Product Listing Page (PLP)** task from the provided Figma design.

---

## 🧩 Framework Used
**Framework:** [Next.js](https://nextjs.org/)  
**Language:** React.js (JavaScript)  

> Next.js was chosen because it supports **Server Side Rendering (SSR)** out of the box, provides easy SEO configuration, and has a simple deployment process with platforms like **Vercel** or **Netlify**.

---

## 🚀 Features

- **Server Side Rendering (SSR):** Implemented using `getServerSideProps()` in Next.js.
- **Responsive Design:** Fully responsive across Desktop, Tablet, and Mobile.
- **Mock API Integration:** Fetched product data from [https://fakestoreapi.com/](https://fakestoreapi.com/).
- **Minimal Dependencies:** Only uses Next.js and React — no heavy external packages.
- **Optimized DOM:** Semantic HTML with `<article>`, `<section>`, `<header>`, `<main>`, and `<footer>`.
- **SEO Settings:**
  - Page Title and Description
  - Canonical Link
  - Proper usage of H1 & H2 tags
  - Schema.org JSON-LD structured data
  - Alt text for all images
  - SEO-friendly image filenames

---

## 📁 Folder Structure

